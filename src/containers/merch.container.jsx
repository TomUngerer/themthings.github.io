import React from "react";

import Section from "../components/section.compontent";

const MerchContainer = (props) => (
  <Section id="merch" title={props.title}>
    <div className="container">
    </div>
  </Section>
);

export default MerchContainer;
